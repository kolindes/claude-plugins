---
description: Show your BUDDY profile and stats
allowed-tools: ["Bash"]
---
Run: `node "${CLAUDE_PLUGIN_ROOT}/scripts/commands.js" status`
Show the output exactly as printed — it contains formatted ASCII art. Do not reformat, summarize, or wrap it in a code block.
