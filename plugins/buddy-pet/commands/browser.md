---
description: Open your BUDDY profile in the browser
allowed-tools: ["Bash"]
---
Run: `node "${CLAUDE_PLUGIN_ROOT}/scripts/commands.js" browser`
Show the output. Tell the user in their language that the link was opened (or provide it if it couldn't open automatically).
