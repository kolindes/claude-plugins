---
description: Show your BUDDY consent settings (which metrics are shared)
allowed-tools: ["Bash"]
---
Run: `node "${CLAUDE_PLUGIN_ROOT}/scripts/commands.js" consents`
Show the output exactly as printed — it's a formatted table. Do not reformat or wrap in a code block.
After showing, briefly explain in the user's language what consent controls do and how they affect XP. If some metrics are OFF, mention the XP impact.
